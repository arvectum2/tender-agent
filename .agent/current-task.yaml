version: 2
repository: arvectum2/tender-agent
status: in_progress
task_id: DOCUMENT-QA-NEXT-INCREMENT
authority: AUTO
goal: Bind EIS analysis attachments to the active/current notice revision only, preserving revision provenance and failing closed on ambiguity.
definition_of_done:
- Parse public EIS notice revision blocks and identify the active/current revision deterministically.
- Bind/download analysis attachments only from the active/current revision; historical revisions stay separate from the current analysis set.
- Preserve revision number/publication timestamp/active state/source URL in provenance.
- Fail closed to review if revision state or attachment binding is ambiguous.
- Add synthetic coverage plus exposed case-14 regression without changing any frozen benchmark artifact.
- Run focused/relevant tests and exact-head CI; prepare review-ready PR and stop before merge unless Product Owner explicitly approves.
completed:
- Owner promoted issue #51 and authorized AUTO implementation/testing with merge REVIEW.
- Claimed isolated branch agent/document-qa-next-increment and inventoried the current SOAP attachment path; it has no revision provenance.
- Reconfirmed case-14 as immutable exposed regression evidence.
- Identified EisDocumentRaw.raw_meta as the backward-compatible revision provenance seam.
- Reconciled issue #51 open and branch checkpoint; no competing executor or completed gate overrides the task.
- Inspected pyproject.toml and repository code search: no dedicated HTML parser dependency/helper is present, so Python stdlib html.parser is the bounded reuse-first parser seam.
- Prepared the standalone fail-closed parser/selector design; the connected GitHub product-file creation action was blocked by its safety layer before code landed.
current: Product implementation is blocked at the first new source-file write; checkpoint commit 5bbe751019c023087863ff7ff057d32a253cd69b records the exact recovery state.
pending:
- Retry/add the bounded standalone revision parser/selector when repository product-file writes are available.
- Add synthetic cases: single revision; inactive+active overlapping filename; changed file; duplicate URL; ambiguity.
- Positively identify real public EIS documents-page revision markers before runtime wiring; never infer active state from DOM order.
- Bind current analysis attachments only to the selected active revision and retain historical revision metadata separately.
- Add exposed case-14 HTML regression without changing frozen benchmark truth/artifacts.
- Run focused/relevant tests and exact-head CI; open review-ready PR.
last_verified_commit: 5bbe751019c023087863ff7ff057d32a253cd69b
test_results:
- No product tests run; product-file creation was blocked before code landed.
updated_at: '2026-09-15T18:14:00Z'
resume_instruction: Retry the standalone parser/selector source-file write first. Keep it dependency-free using Python html.parser because no repository HTML parser dependency/helper was found. Require explicit revision state and exactly one active revision; fail closed rather than infer from DOM order. Then add synthetic tests before runtime wiring. Preserve case-14 frozen artifacts byte-for-byte and leave generic SOAP semantics unchanged.
execution:
  claimed_by: Tender Agent Watchdog
  execution_id: watchdog-20260915T2158MSK
  branch: agent/document-qa-next-increment
  base_commit: d41ea1c43b649160cc9ef6579f1f36a116e0bec6
  issue: 51
  manual_override: false
  lease_until: '2026-09-15T20:04:00Z'
