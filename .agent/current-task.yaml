version: 2
repository: arvectum2/tender-agent
status: in_progress
task_id: PROCUREMENT-KANBAN-V1-001
authority: AUTO
goal: Implement a thin operator kanban inside the existing Commercial Operator Console using canonical Deal/DealStatus state and transition rules, without introducing parallel workflow semantics or external effects.
definition_of_done:
- Add an internal kanban route/view in the existing Commercial Operator Console.
- Group active non-deleted deals deterministically by canonical DealStatus; archived/deleted semantics remain consistent with existing behavior.
- Show compact cards from already persisted deal fields and link to the existing tender/deal card.
- Add bounded filters/search using existing persisted fields only.
- If status changes are exposed, they are explicitly human-initiated and call the existing canonical status validation/apply path; invalid transitions fail closed and remain audited.
- Do not add automatic transitions, procurement submission, external communication, CRM clone behavior, a parallel stage enum, a new workflow table, a new frontend framework, or an unnecessary DB migration.
- Focused kanban/status/deal/console tests and relevant regression tests pass; exact-head CI is green.
- Prepare a review-ready PR; final merge is not implicitly approved and remains gated because auto_merge=false.
completed:
- Recovered canonical roadmap/queue/checkpoint state and reconciled open PR #84 plus recommended issue #83.
- Product Owner directive on 2026-09-18 admitted issue #83 as the next bounded roadmap increment; durable admission recorded in issue comment #5726117047.
- Created isolated branch agent/procurement-kanban-v1-001 from main aa5aa7aed45bcbac40fb95fe802bcbdc53b3649c.
- Added PROCUREMENT-KANBAN-V1-001 to the canonical execution queue as order 90, AUTO, auto_merge=false, mapped to ARV-019.
current: Inventory existing Commercial Operator Console, Deal/DealStatus model, canonical transition service, templates/styles and tests before implementing the smallest COPY_PATTERN slice.
pending:
- Inventory reusable console/status/deal patterns and freeze the implementation seam.
- Implement kanban board and bounded filters/search without new workflow state.
- Add/adjust focused tests proving deterministic grouping, deleted/archive handling and canonical transition enforcement if transition controls are exposed.
- Run relevant validation and inspect exact-head CI.
- Open/update a review-ready PR and stop before merge unless separately authorized.
last_verified_commit: aa5aa7aed45bcbac40fb95fe802bcbdc53b3649c
test_results:
- "No code tests run yet; task admission/bootstrap only."
updated_at: '2026-09-18T09:28:00+03:00'
resume_instruction: Inspect the existing Commercial Operator Console routes/templates, Deal/DealStatus model and canonical status-transition service on this branch; then implement the smallest server-rendered kanban slice that reuses them and add focused tests.
execution:
  claimed_by: ChatGPT interactive
  execution_id: interactive-20260918-procurement-kanban-v1
  branch: agent/procurement-kanban-v1-001
  base_commit: aa5aa7aed45bcbac40fb95fe802bcbdc53b3649c
  issue: 83
  manual_override: false
  lease_until: '2026-09-18T11:18:00+03:00'
