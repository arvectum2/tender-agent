version: 2
repository: arvectum2/tender-agent
status: in_progress
task_id: OPS-OBSERVABILITY-V1-001
authority: AUTO
goal: Expose repository-native, machine-readable operational observability for queue, ingestion, document
  processing, storage guard, backup state and recent worker errors, with deterministic status rules and
  non-destructive recovery guidance.
definition_of_done:
- Versioned operational metrics/status schema and endpoint/CLI surface.
- Queue age/depth, ingestion, storage and backup signals sourced from existing state where supported.
- Explicit UNKNOWN/UNAVAILABLE for unsupported metrics rather than fabricated zeroes.
- Regression tests for healthy, degraded, stale queue, storage guard and unavailable dependency states.
- Runbook with diagnostic and non-destructive recovery steps.
- Focused/relevant tests and exact-head CI green.
completed:
- 'Read issue #68 and queue admission; task is independent AUTO with auto_merge=true and no dependencies.'
- Created isolated branch/worktree agent/ops-observability-v1-001 from main f060fe23a89d3334cf3d776062e980b4a0945e7c.
- 'Reuse inventory: src/main.py /health/ready already exposes storage_metrics_dict and Redis health; ops
  snapshot will reuse public storage state rather than duplicate disk logic.'
- 'Reuse inventory: TenderAnalysisJob persists queued/running/failed job state and timestamps suitable
  for queue depth/oldest age and recent worker-error counters.'
- 'Reuse inventory: DocumentIngestionRun persists started/completed/partial/failed ingestion runs; ProcurementTenderDocument
  persists download/text-extraction failures and updated_at.'
- 'Reuse inventory: ARV-076 backup tooling writes BACKUP_COMPLETE plus MANIFEST.json with created_at;
  no runtime backup-root setting exists, so backup metrics must be optional and explicitly UNAVAILABLE
  when not configured.'
- 'Bounded design selected: add a no-migration ops_observability module with versioned snapshot schema,
  GET /api/ops/observability endpoint, deterministic thresholds, optional ARVECTUM_BACKUP_ROOT metadata
  reader, and runbook. No third-party telemetry or destructive recovery.'
current: Implement the no-migration ops_observability module and optional backup-root setting using only
  existing persisted state and public storage snapshot.
pending:
- Add versioned schemas/service/router and register /api/ops/observability.
- Add optional ARVECTUM_BACKUP_ROOT setting; expose backup as UNAVAILABLE/UNKNOWN when not configured
  or no completed backup exists.
- Add healthy/degraded/stale queue/storage guard/unavailable dependency regressions plus runbook.
- Run focused/relevant tests and exact-head CI; apply guarded auto-merge gates only if AM-4 remains provably
  valid.
last_verified_commit: f060fe23a89d3334cf3d776062e980b4a0945e7c
test_results:
- 'No #68 product tests run yet; inventory stage only.'
updated_at: '2026-09-16T08:39:16+03:00'
resume_instruction: Implement src/modules/ops_observability with a versioned snapshot built from TenderAnalysisJob,
  DocumentIngestionRun, ProcurementTenderDocument and public_storage_snapshot(). Add optional ARVECTUM_BACKUP_ROOT
  safe metadata lookup and /api/ops/observability. Preserve UNKNOWN/UNAVAILABLE instead of zero for unsupported/no-source
  conditions; no third-party telemetry or recovery mutation.
execution:
  claimed_by: ChatGPT interactive
  execution_id: interactive-20260916-ops-observability
  branch: agent/ops-observability-v1-001
  base_commit: f060fe23a89d3334cf3d776062e980b4a0945e7c
  issue: 68
  manual_override: true
  lease_until: '2026-09-16T10:29:16+03:00'
