version: 2
repository: arvectum2/tender-agent
status: in_progress
task_id: BUILD-DOCKER-CONTEXT-001
authority: AUTO
goal: "Restore a self-contained, reproducible clean-checkout Docker build by removing or replacing the undeclared dependency on arvectum-landing/public/assets."
definition_of_done:
  - "Trace whether arvectum-landing/public/assets is actually required at Tender Agent runtime and identify the intended source of any required assets."
  - "Clean checkout + docker build . succeeds without local sibling-directory assumptions."
  - "No credentials or network-only mutable artifacts are embedded implicitly."
  - "Runtime image still contains every asset actually required by the application."
  - "Relevant CI/deployment documentation or smoke coverage is updated if needed, and required exact-head/current-base gates pass before merge."
completed:
  - "REUSE-FIRST-001 completed and merged in PR #56; Owner renewed POS-004 / AI-ENG-001 on unchanged terms."
  - "Execution queue reconciled to select BUILD-DOCKER-CONTEXT-001 as the next admitted ready AUTO workstream."
  - "Issue #19 acceptance criteria reviewed."
  - "Runtime trace confirmed landing files are optional: site_public_root defaults to None and install_optional_site_mount returns without mounting when unset; docker-compose tender-pilot does not set it."
  - "Repository trace confirmed main has no arvectum-landing directory, while Dockerfile and local preview compose assumed one."
  - "Branch agent/build-docker-context-001 removes the invalid Dockerfile COPY, makes local marketing-site path explicit through required ARVECTUM_LANDING_PUBLIC_DIR, updates deployment guidance, and adds clean-checkout docker build CI."
  - "PR #58 opened from exact branch head 60c1a054787d8e3c48d1fd3a3ffc0ac9c14df622."
current: "Validate PR #58 exact head/current base, including the new clean-checkout docker-build CI job."
pending:
  - "Verify all required CI jobs are green for PR #58 exact head."
  - "Recheck mergeability/current-base state after CI."
  - "Merge #58, confirm issue #19 closes, mark checkpoints/queue completed, then advance only to the next explicitly admitted AUTO item."
last_verified_commit: "60c1a054787d8e3c48d1fd3a3ffc0ac9c14df622"
test_results:
  - "Static/runtime inspection: optional site mount is disabled unless AI_CORP_SITE_PUBLIC_ROOT is explicitly configured."
  - "PR #58 adds docker-build CI as the clean-checkout acceptance regression gate; CI result pending."
updated_at: "2026-09-15T09:30:00Z"
resume_instruction: "Continue BUILD-DOCKER-CONTEXT-001 at PR #58. Do not redo runtime tracing. Verify exact-head CI for 60c1a054787d8e3c48d1fd3a3ffc0ac9c14df622 and current-base mergeability. If green, squash-merge, verify issue #19 closure, set task/checkpoint/queue to completed, then inspect the queue for the next explicitly admitted AUTO workstream. Do not promote REVIEW/deferred items yourself."
execution:
  claimed_by: "manual Owner-requested roadmap continuation"
  execution_id: "build-docker-context-001-20260915"
  branch: "agent/build-docker-context-001"
  pull_request: 58
  manual_override: true
