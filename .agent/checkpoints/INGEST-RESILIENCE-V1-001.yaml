version: 2
repository: arvectum2/tender-agent
status: in_progress
task_id: INGEST-RESILIENCE-V1-001
authority: AUTO
goal: Harden public procurement ingestion with resumable bounded sync, deterministic
  idempotent replay, and revision-aware source identity/provenance without source-side
  mutation.
definition_of_done:
- Durable resumable cursor/checkpoint or equivalent bounded restart mechanism.
- Deterministic idempotent replay for tenders, documents and source metadata.
- Revision-aware identity model preventing inactive/current attachment mixing where
  source state is known.
- Tests for interrupted batch resume, duplicate replay, changed revision, conflicting
  identity and unavailable source.
- Existing public 44-FZ flow remains compatible; focused/relevant tests and exact-head
  CI pass.
- Operational docs describe recovery semantics and observable failure states.
completed:
- 'Issue #66 and queue admission read; dependency DOCUMENT-QA-NEXT-INCREMENT is satisfied
  by merged PR #73 / closed issue #51 despite stale queue wording.'
- Initial reuse inventory identified existing TenderRepository tender/document upsert
  and identity-hash dedupe, TenderPipeline EIS ingestion, current-revision public
  44-FZ binding, and existing stateless public-search cursor primitives as reuse seams.
- Created isolated branch/worktree agent/ingest-resilience-v1-001 from current main
  f060fe23a89d3334cf3d776062e980b4a0945e7c.
current: Inventory exact ingestion transaction boundaries and document identity semantics;
  define the smallest durable sync/checkpoint seam before product code changes.
pending:
- Inspect _save_one_tender and document identity implementation plus migration/model
  patterns; choose bounded durable sync state schema.
- Implement resumable sync/checkpoint and revision-aware idempotent repository behavior
  without a second ingestion stack.
- Add interrupted-resume, duplicate, changed-revision, conflicting-identity and unavailable-source
  regressions plus operational docs.
- Run focused/relevant tests and exact-head CI; prepare/merge only under valid AUTO
  + AM-4 gates.
last_verified_commit: f060fe23a89d3334cf3d776062e980b4a0945e7c
test_results:
- 'No #66 product tests run yet; inventory stage only.'
updated_at: '2026-09-16T07:58:35+03:00'
resume_instruction: Inspect TenderPipeline._save_one_tender, TenderRepository._compute_identity_hash/upsert_document,
  and current tender/document migrations. Add a bounded checkpoint before code changes;
  reuse existing repository/dedupe primitives and do not create source-side mutations
  or silent ambiguous-revision fallbacks.
execution:
  claimed_by: ChatGPT interactive
  execution_id: interactive-20260916T075835+0300
  branch: agent/ingest-resilience-v1-001
  base_commit: f060fe23a89d3334cf3d776062e980b4a0945e7c
  issue: 66
  manual_override: true
  lease_until: '2026-09-16T09:48:35+03:00'
checkpoint_role: durable_task_checkpoint
