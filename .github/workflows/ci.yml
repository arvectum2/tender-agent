name: CI
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  dependency-lock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Install pinned uv for lock verification
        run: python -m pip install 'uv==0.12.8'
      - name: Verify requirements.lock matches pyproject.toml
        run: |
          uv pip compile pyproject.toml --extra dev --python-version 3.11 --universal --generate-hashes --output-file requirements.lock
          git diff --exit-code -- requirements.lock

  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      # PDF assertions use pdftotext; R9 recovery acceptance uses pg_dump/pg_restore.
      - run: sudo apt-get update && sudo apt-get install --yes poppler-utils postgresql-client
      - name: Install locked Python dependencies
        run: python -m pip install --require-hashes -r requirements.lock
      - run: make check
      - name: Run make test
        shell: bash
        run: |
          mkdir -p output
          set -o pipefail
          make test 2>&1 | tee output/pytest.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: r9-operational-hardening-evidence-${{ github.event.pull_request.head.sha || github.sha }}
          if-no-files-found: error
          path: |
            output/*.log
            output/r9-db-filesystem-mismatch-*/
            output/r9-orphan-lifecycle-*/
            output/r9-backup-restore-*/

  p6-03-arvectum-os-bridge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Checkout arvectum-os
        uses: actions/checkout@v4
        with:
          repository: arvectum2/arvectum-os
          ref: 0e0fcac9a2fbf43920799ba96110a0db149cb85b
          path: arvectum-os-ref
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Install locked Python dependencies
        run: python -m pip install --require-hashes -r requirements.lock
      - name: Run P6.03 bridge tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/arvectum-os-ref/reference/python
          python -m pytest -v tests/test_p6_03_arvectum_os_bridge.py

  migrations:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Install locked Python dependencies
        run: python -m pip install --require-hashes -r requirements.lock
      - run: alembic heads

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/ops/secret_scan.py

  r8-postgres-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: sudo apt-get update && sudo apt-get install --yes poppler-utils
      - name: Install locked Python dependencies
        run: python -m pip install --require-hashes -r requirements.lock
      - run: make test-r8-postgres
      - run: make test-r8-acceptance-migration-backfill
        env:
          GITHUB_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      - run: make test-r8-acceptance-tampering
        env:
          GITHUB_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: r8-migration-backfill-evidence-${{ github.event.pull_request.head.sha || github.sha }}
          path: output/r8-acceptance-migration-backfill-*/
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: r8-tampering-evidence-${{ github.event.pull_request.head.sha || github.sha }}
          path: output/r8-acceptance-tampering-*/

  r8-acceptance-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: sudo apt-get update && sudo apt-get install --yes poppler-utils
      - name: Install locked Python dependencies
        run: python -m pip install --require-hashes -r requirements.lock
      - run: make test-r8-acceptance-tenant-concurrency
        env:
          GITHUB_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: r8-tenant-evidence-${{ github.event.pull_request.head.sha || github.sha }}
          path: output/r8-acceptance-tenant-concurrency-*/

  redis-integration:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7.4.9-alpine
        ports:
          - 16379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install locked Python dependencies
        run: python -m pip install --require-hashes -r requirements.lock
      - run: make test-redis-integration
        env:
          AI_CORP_REDIS_ENABLED: "true"
          AI_CORP_REDIS_URL: redis://127.0.0.1:16379/1
          AI_CORP_REDIS_NAMESPACE: ci-${{ github.sha }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: redis-integration-logs-${{ github.event.pull_request.head.sha || github.sha }}
          if-no-files-found: warn
          path: output/*.log
